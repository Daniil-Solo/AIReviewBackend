Вот список вопросов, направленных на выявление слабых мест, неточностей и пробелов в предоставленной документации:

**C1.** В разделе «Структура директорий» указан файл `Parsing/configs.json`, а в разделе «Конфигурация проекта» перечислены константы `LOOP_DELAY` как жестко прописанные в файлах `AvitoParser.py` и `Page.py`. Какая из этих двух конфигураций является актуальной? Если используется `configs.json`, почему константы `LOOP_DELAY` указаны в разделе «Основные константы»?

**C2.** В диаграмме классов `functions` отображается как класс, но в описании архитектуры и структуре проекта `functions.py` указан как модуль. Является ли `functions` классом (singleton?), или это опечатка в диаграмме? Если это модуль, какова его ответственность по отношению к классу `BestModel`?

**C3.** В разделе «Особенности реализации» упоминается, что «One-hot кодирование в GUI» происходит вручную. Обычно кодирование признаков производится на этапе подготовки данных (`Preparing_data`). Уточните, действительно ли кодирование происходит внутри графического интерфейса, или это означает, что GUI передает сырые данные, а кодирование происходит внутри функции `get_price` перед подачей в модель?

**C4.** В разделе «Тестирование» диаграмма пирамиды тестирования показывает, что модульные тесты покрывают 100% проекта, в то время как текстовое описание утверждает, что тесты есть только для обработчиков парсинга (модульные тесты). Уточните, покрывают ли модульные тесты только модуль `Parsing` или весь проект целиком? Если только `Parsing`, почему диаграмма подразумевает покрытие всей системы?

**C5.** В разделе «Наблюдаемость системы» указано, что логирование реализовано через `print()`. Уточните, предусмотрена ли возможность настройки уровня детализации логов (DEBUG, INFO, WARNING) или перенаправления вывода в файл, так как использование `print()` в продакшене или при длительном запуске парсера затрудняет отладку?

**C6.** В разделе «Проблемы и зоны развития» упоминается ошибка в синтаксисе исключений `except AttributeError or TypeError`. Уточните, является ли этот код (с ошибкой) частью текущей версии в репозитории, или это описание потенциальной уязвимости, которую еще не исправили? Если это ошибка, где именно в коде она находится?

**C7.** В описании модуля `Prediction` указано, что модель загружается из файла при каждом вызове функции `get_price()`. Уточните, планируется ли внедрение паттерна «Singleton» или кэширования модели для оптимизации производительности, так как текущая реализация приведет к значительным накладным расходам при частых запросах?

**C8.** В разделе «Конфигурация проекта» указано, что переменные окружения отсутствуют, а все конфигурации хранятся в JSON-файлах. Уточните, как проект обрабатывает различия в окружениях (разработка, тестирование, продакшен)? Используется ли ветвление файлов конфигурации или жестко прописанные пути к файлам?

**C9.** В диаграмме последовательности «Предсказание стоимости» участник `Amenities` вызывается как отдельный компонент, но в структуре проекта `amenities.json` — это просто файл данных. Уточните, является ли `Amenities` классом, который загружает этот файл, или это просто абстракция для диаграммы, подразумевающая работу с модулем `functions`?

**C10.** В разделе «Особенности реализации» описан расчет расстояния с использованием формулы гаверсинуса. Уточните, используется ли эта функция для расчета расстояния до центра города (Перми) или до объектов инфраструктуры? Если до инфраструктуры, как учитывается, что некоторые объекты могут находиться в подвале или на крыше здания, где расстояние до них не равно геодезическому?