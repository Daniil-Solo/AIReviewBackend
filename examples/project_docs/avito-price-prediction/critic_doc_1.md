A1. Указано, что в `except AttributeError or TypeError:` есть ошибка, но почему в разделе "Проблемы и зоны развития" это не выделено как критическая проблема с пометкой о необходимости срочного исправления, если такой код приведёт к неперехвату исключений и падению приложения?

A2. Почему в разделе "Зависимости и технологический стек" указана версия `bs4==0.0.1`, которая явно некорректна (такой версии не существует), и при этом не указано, что это ошибка в `requirements.txt`, требующая проверки и исправления?

A3. В диаграмме внутренних компонентов (`classDiagram`) класс `Distributor` указан как возвращающий `AbstractHandler`, но в описании не раскрыто, как именно происходит выбор конкретного обработчика — можно ли считать это достаточным для понимания логики распределения, или требуется уточнение механизма сопоставления ключа и обработчика?

A4. В диаграмме последовательности "Сбор данных с Avito" участник `Handler` представлен как единый блок, хотя на самом деле используется множество конкретных обработчиков. Не вводит ли это в заблуждение относительно архитектуры, и не следует ли заменить его на `Distributor → [конкретный Handler]` или добавить пояснение?

A5. В описании структуры проекта указано, что `Preparing_data` и `Modelling` содержат Jupyter-ноутбуки, но не указаны их имена и пути — как ревьювер может найти эти файлы для анализа, если они критичны для воспроизводимости?

A6. Почему в разделе "Конфигурация проекта" не приведён пример содержимого `configs.json`, если он используется как основной конфигурационный файл парсера и его структура влияет на работу всей системы?

A7. В диаграмме архитектуры стрелка от `amenities.json` идёт напрямую в `Prediction GUI`, но на самом деле данные обрабатываются через `functions.py`. Не противоречит ли это реальной архитектуре, и не следует ли добавить промежуточный узел `functions`?

A8. В описании `LOOP_DELAY` указаны два разных значения (5 и 7 секунд) в разных файлах — это преднамеренное различие или потенциальный источник путаницы? Если преднамеренно, требуется ли пояснение логики выбора разных задержек?

A9. В разделе "Особенности реализации" упоминается one-hot кодирование в GUI, но не указано, как именно оно реализовано (вручную, через словари, списки) и совпадает ли порядок признаков с тем, который использовался при обучении модели — может ли это привести к ошибкам предсказания?

A10. В диаграмме внутренних компонентов `functions` представлен как класс, но в проекте это, скорее всего, модуль с функциями. Не вводит ли использование `classDiagram` в заблуждение относительно ООП-структуры, и не следует ли переименовать `functions` в `PredictionUtils` или аналогичное, либо изменить диаграмму на `componentDiagram`?

A11. В разделе "Тестирование" указано, что тесты находятся в `Parsing/parsing_tests/handlers_test.py`, но в структуре проекта указан каталог `parsing_tests/` без уточнения полного пути — соответствует ли это реальной структуре, и можно ли однозначно определить расположение файла?

A12. Почему в диаграмме изменения статусов указано "Не применимо", если даже статические данные могут иметь статусы (например, "в процессе парсинга", "обработано", "ошибка")? Не упущена ли возможность описать жизненный цикл данных?

A13. В разделе "Наблюдаемость системы" отмечено отсутствие логирования, но не указано, в каких файлах и на каких этапах используются `print()` — можно ли однозначно определить, где требуется замена на `logging`?

A14. В последовательности "Предсказание стоимости" не отражена обработка ошибок (например, при невалидных координатах или отсутствии модели) — не следует ли добавить ветвление с ошибками для полноты сценария?

A15. В описании `BestModel` указано, что модель сохранена как `best_model.pkl`, но в разделе "Зависимости" не указано, как именно сериализуется модель (pickle, torch.save) — может ли это повлиять на совместимость и безопасность загрузки?

A16. В разделе "Архитектура" утверждается, что проект близок к слоистой архитектуре, но между `Modelling` и `Prediction` нет чёткого интерфейса — как обеспечивается согласованность формата данных между ними, и описано ли это где-либо?

A17. В описании `get_amenities` указан радиус 5 км, но в коде (`functions.py`) он не зафиксирован как константа — не приведёт ли это к рассинхронизации при изменении логики, и почему он не вынесен в конфигурацию?

A18. В разделе "Зоны развития" упомянута необходимость асинхронности, но не указано, какие компоненты в первую очередь выиграют от этого (например, парсинг объявлений) — можно ли уточнить приоритеты оптимизации?

A19. В описании `Distributor` не указано, что происходит, если передан неизвестный ключ — возвращается `EmptyHandler`, но это не отражено в диаграмме и не описано в тексте. Не является ли это скрытой логикой, требующей документирования?

A20. В структуре проекта указаны `*.csv`, но не указано, какие именно CSV-файлы создаются и где они используются — можно ли однозначно определить входные и выходные данные каждого этапа?