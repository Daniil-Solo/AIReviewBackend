## Вопросы для ревьювера

### 1. Структура и полнота

1. **Отсутствие указания путей к файлам в разделе эндпоинтов**: В разделе 4 перечислены все API-эндпоинты, но не указано, в каких файлах они реализованы. Рекомендуется добавить колонку с путями к файлам (например, `src/api/courses/courses.py`) для быстрой навигации.

2. **Отсутствие путей к файлам с примерами кода**: В разделе 6 "Особенности реализации" приведены 4 блока кода с важными алгоритмами, но не указано, в каких файлах они находятся. Рекомендуется добавить комментарии с путями к файлам (например, `# Исходный код: src/domain/course_run/course_run.py`).

3. **Неполнота описания конфигурации**: В разделе 5 перечислены константы и переменные окружения, но не указано, где они определены в коде. Где именно хранятся эти конфиги?

### 2. Ясность и понятность

4. **Противоречие в терминологии "запуск курса"**: В глоссарии (раздел 1) указан термин "Запуск курса (course run)", но в схеме БД (раздел 3) присутствует только таблица `course_runs_ref`, а не `course_runs`. Это одно и то же или разные сущности?

5. **Противоречие в терминологии "правило расписания"**: В глоссарии указано "Правило расписания (rule)", но в схеме БД используется `timetable_rules`. Какой термин является каноническим?

6. **Несоответствие диаграммы последовательности аутентификации**: На диаграмме последовательности для аутентификации (раздел 3) отсутствует вызов `Security.verify_password()`, хотя в разделе 6 "Особенности реализации" этот компонент упоминается. Следует ли добавить этот шаг на диаграмму?

### 3. Точность и непротиворечивость

7. **Ошибка в схеме БД - таблица course_runs**: В схеме БД указана таблица `course_runs` с полями `id`, `course_id`, `name`, `is_archive`, `created_at`, `updated_at`, но в er-диаграмме эта таблица не отображена корректно (есть только `course_runs_ref`). Какова правильная структура?

8. **Ошибка в схеме БД - связь course_runs**: В диаграмме указана связь `courses ||--o{ course_runs : "запуски"`, но в описании таблиц эта связь не раскрыта. Следует добавить полное описание таблицы `course_runs`.

9. **Несоответствие в диаграмме состояний**: В диаграмме изменения статусов (раздел 3) для Feedback и CourseRun указаны состояния Active и Archived, но в легенде диаграммы эти состояния не отображены. Следует ли добавить их в диаграмму?

### 4. Визуализация

10. **Диаграмма компонентов неполная**: На диаграмме внутренних компонентов (раздел 3) показаны связи, но не отражено, как доменные сущности взаимодействуют с сервисами. Рекомендуется добавить стрелки от домена к сервисам.

11. **Отсутствие диаграммы пакетов**: Для понимания структуры проекта полезно иметь диаграмму пакетов (package diagram), показывающую зависимости между модулями (`api`, `domain`, `services`, `infrastructure`). Планируется ли её добавить?

12. **Диаграмма последовательности аутентификации упрощена**: На диаграмме отсутствует этап проверки пароля через Security-компонент, хотя в разделе 6 этот компонент описан. Следует ли уточнить диаграмму?

### 5. Практическая значимость для ревьювера

13. **Отсутствие указания на основные файлы-обработчики**: Для быстрого старта ревьювера полезно знать, какие файлы являются точками входа. Указаны `src/app.py` и `src/config.py`, но не указаны ключевые файлы вроде `src/api/health_check.py` или основные роутеры.

14. **Неполный перечень зависимостей**: В разделе 2 перечислены основные библиотеки, но отсутствуют важные зависимости, используемые в коде (например, `pydantic-settings`, `passlib`, `python-jose` и др.). Где можно найти полный перечень?

15. **Отсутствие информации о миграциях**: В разделе 3 указана директория `alembic/versions/`, но не указано, какие миграции существуют и какие таблицы они создают. Есть ли отдельная документация по миграциям?

### 6. Дополнительные уточнения

16. **Раздел "Наблюдаемость системы" (раздел 7)**: Указано, что логирование, мониторинг и трейсинг отсутствуют. Это намеренное решение или временное? Есть ли планы по их добавлению?

17. **Раздел "Особенности реализации" (раздел 6)**: Упомянуты паттерны Factory Method в ORM-моделях с методами `from_domain` и `to_domain`. В каких файлах это реализовано?

18. **Раздел "Тестирование" (раздел 8)**: Указано, что нет тестов для проверки пересечения расписаний. Это известный пробел или намеренно?

19. **Раздел "Проблемы и зоны развития" (раздел 9)**: Пункт 3 указывает на проблему с фильтрацией курсов - она происходит в памяти, а не на уровне БД. Где именно в коде это происходит?

20. **Кэширование**: В разделе 5 указаны параметры TTL для кэша (`TIME_TO_LIVE_ALL_COURSES`, `TIME_TO_LIVE_ONE_COURSE` и др.), но не указано, где в коде реализовано кэширование. В каких файлах находится логика кэширования?