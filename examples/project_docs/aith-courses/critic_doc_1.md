1. **Почему в диаграмме верхнеуровневой архитектуры Google Calendar и внешние API показаны как входящие зависимости в FastAPI, а не как внешние сервисы, вызываемые приложением?**  
   *Рекомендация:* Уточните направление взаимодействия: скорее всего, приложение инициирует запросы к Google Calendar, а не наоборот. Исправьте стрелки на `B --> G` и `B --> H`, чтобы избежать путаницы.

2. **Почему в схеме базы данных отсутствуют явные связи между `course_runs` и `timetable_rules`, `group_google_calendars`, `playlists`, несмотря на наличие внешних ключей?**  
   *Рекомендация:* Добавьте связи `course_runs ||--o{ timetable_rules`, `course_runs ||--o{ group_google_calendars`, `course_runs ||--o{ playlists` в ER-диаграмму для полноты отображения структуры данных.

3. **Почему в описании API-эндпоинтов не указаны пути к соответствующим файлам с обработчиками (например, `src/api/courses/...`)?**  
   *Рекомендация:* Укажите полные пути до файлов с эндпоинтами, чтобы ревьювер мог быстро перейти к реализации (например, `POST /api/v1/courses → src/api/courses/handlers.py`).

4. **Почему в описании структуры проекта отсутствует папка `schemas/` или `dtos/`, хотя активно используются Pydantic-модели (`CreateCourseRequest`, `CourseFullDTO` и т.д.)?**  
   *Рекомендация:* Либо добавьте раздел с описанием расположения DTO и схем валидации, либо укажите, что они находятся внутри соответствующих модулей API (например, `src/api/courses/schemas.py`).

5. **Почему в диаграмме внутренних компонентов слой "Домен" не соединён со слоем "Сервисы", несмотря на то, что сервисы используют сущности и исключения из домена?**  
   *Рекомендация:* Добавьте стрелки от `H`, `I`, `J` (сущности, значения, исключения) к `B (Сервисы)`, чтобы отразить реальную зависимость и улучшить понимание потока данных.

6. **Почему в описании конфигурации не указано, где и как происходит загрузка переменных окружения (например, через `pydantic-settings` или `dotenv`)?**  
   *Рекомендация:* Уточните, какой механизм используется для загрузки `.env`-файлов и как конфигурация инжектируется в приложение (например, в `src/config.py`).

7. **Почему в описании паттернов проектирования не указано, где именно реализованы интерфейсы репозиториев (например, `ICourseRepository`) и как происходит их подмена (например, для тестов)?**  
   *Рекомендация:* Укажите, где находятся интерфейсы и конкретные реализации, и как реализована инъекция зависимостей (например, через фабрику сервисов или DI-контейнер).

8. **Почему в описании кэширования не указано, какие именно данные кэшируются и по каким ключам (например, `course:{id}`, `courses:all:filtered:{hash}`)?**  
   *Рекомендация:* Добавьте таблицу с примерами ключей кэша и их TTL, чтобы прояснить стратегию кэширования и помочь в диагностике проблем с согласованностью.

9. **Почему в разделе "Особенности реализации" не описано, как и где используется `Unit of Work`, и какие репозитории он координирует в конкретных операциях?**  
   *Рекомендация:* Приведите пример использования `UnitOfWork` в сервисе (например, `CourseCommandService`), чтобы показать, как обеспечивается атомарность операций.

10. **Почему в описании тестов не указано, как запускаются интеграционные тесты с Docker (например, через `docker-compose -f test.docker-compose.yaml up -d`)?**  
    *Рекомендация:* Добавьте команду для поднятия инфраструктуры перед запуском интеграционных тестов, чтобы обеспечить воспроизводимость.

11. **Почему в диаграмме изменения статусов отсутствуют переходы для сущностей `Feedback` и `CourseRun`, несмотря на наличие `is_archive` флагов?**  
    *Рекомендация:* Уточните, какие именно действия вызывают архивацию отзывов и запусков курсов, и добавьте соответствующие переходы в диаграмму.

12. **Почему в описании эндпоинтов отсутствует информация о правах доступа (например, `@admin_required`, `@auth_required`) для каждого обработчика?**  
    *Рекомендация:* Добавьте столбец "Требуемая роль" в таблицу API-эндпоинтов, чтобы быстро понимать, кто может вызывать каждый эндпоинт.

13. **Почему в описании алгоритма `is_actual_by_date` не указано, откуда берётся `self.name.season` и `self.name.year` (например, из составного поля `name`)?**  
    *Рекомендация:* Уточните структуру `CourseRun.name` и, при необходимости, добавьте пример значения (например, "Осень 2024").

14. **Почему в разделе "Наблюдаемость" не указано, что отсутствие логирования может затруднить диагностику ошибок в production, и какие минимальные логи стоит добавить (например, запросы, ошибки, длительные операции)?**  
    *Рекомендация:* Дополните раздел конкретными рекомендациями по внедрению логирования (например, middleware для логирования запросов и ответов).

15. **Почему в описании проблемы с кэшированием не предложено конкретное решение (например, инвалидация `courses:all` при создании/удалении курса)?**  
    *Рекомендация:* Уточните, как именно можно синхронизировать кэши (например, через событийный механизм или прямой вызов `cache.delete("courses:all")` в командном сервисе).